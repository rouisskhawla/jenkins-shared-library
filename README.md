# jenkins-shared-library
This repository contains a Jenkins Shared Library used to standardize and simplify CI/CD pipelines. Instead of duplicating pipeline code in every project, all common logic (build, test, versioning, and deployment) lives here and can be reused across multiple services.
